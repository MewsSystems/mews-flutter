name: storybook
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  optimus:
    path: ../optimus
  rxdart: ^0.24.0
  storybook_flutter: ^0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mews_pedantic: ^0.1.1

flutter:
  uses-material-design: true
