name: kiosk_mode_example
description: Demonstrates how to use the kiosk_mode plugin.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  kiosk_mode:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  mews_pedantic: ^0.17.0
flutter:
  uses-material-design: true
